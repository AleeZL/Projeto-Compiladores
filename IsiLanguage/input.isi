programa

    numero a, b.
    texto c.
    leia(a).
    leia(b).
    
    leia(c).

    a := 1.
    b := 2.
    
    se (a > b) {
        escreva(a).
    } senao {
        escreva(b).
    }
    
fimprog.
