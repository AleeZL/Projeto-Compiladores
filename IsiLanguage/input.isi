programa

    numero a,b, k.
    texto c.
    texto d.

    leia(a).
    leia(b).
    
    leia(c).
    
    d := "Isto nao eh um texto!".
    a := 5.
    b := 2.
    k := 7.
    c := "eita!+".
    
    se (b > a) {
        escreva(a).
    } senao {
        leia(c).
    }

    enquanto (a > b) {
        a := a - 10.
    }

    escreva(d).
    
fimprog.